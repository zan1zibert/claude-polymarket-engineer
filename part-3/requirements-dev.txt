# Dev/test dependencies — NOT installed into the service images (they use
# requirements.txt). Install locally to run the test suite:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest                      # unit tests (DB tests skip without TEST_DATABASE_URL)
#   TEST_DATABASE_URL=postgresql://pm:pm@localhost:5432/pm pytest   # + DB tests
pytest
