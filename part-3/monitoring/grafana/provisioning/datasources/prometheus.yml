# Provisioned Prometheus datasource — Grafana picks this up on startup so the
# stack comes up wired together with no manual clicking.
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
